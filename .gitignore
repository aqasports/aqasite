dist/
node_modules/
uploads/
messages.json
*.apk
*.bat
*.ps1
package-loc

# Environment files — NEVER commit these
.env
.env.local
.env.production
.env.development
.env.*.local

# Backup files
*.bak
*-backup.*
*_backup.*

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Local Netlify folder
.netlify

# Deploy trigger — must never be committed (would cause phantom deploys on pull)
deploy.trigger

# Admin panel secret configuration (now tracked for serverless token validation)
# admin-config.json is allowed to be tracked

# Large media files — too big for GitHub (>100MB), must be hosted separately
*.mov
*.mp4


# Old translation system — deleted
auto-translate.js

# OS files
.DS_Store
Thumbs.db

# Editor files
.vscode/
*.swp
*.swo

# Windows AppData
%AppData%

# Agent skills dependencies
.agents/

