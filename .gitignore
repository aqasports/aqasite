node_modules/
uploads/

# Environment files — NEVER commit these
.env
.env.local
.env.production
.env.development
.env.*.local

# Backup files
*.bak
*-backup.*
*_backup.*

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Local Netlify folder
.netlify

# Deploy trigger — must never be committed (would cause phantom deploys on pull)
deploy.trigger

# Admin panel secret configuration (now tracked for serverless token validation)
# admin-config.json is allowed to be tracked

# Large media assets — must be hosted separately or served locally
public/events/Fav main vid.mov

# Old translation system — deleted
auto-translate.js

# OS files
.DS_Store
Thumbs.db

# Editor files
.vscode/
*.swp
*.swo
