---
import BaseLayout from '../../layouts/BaseLayout.astro';
import Navbar from '../Navbar.astro';
import Footer from '../Footer.astro';

import frDict from '../../../lang/fr.json';
import arDict from '../../../lang/ar.json';
import enDict from '../../../lang/en.json';

export interface Props {
  lang?: string;
}

const { lang = 'fr' } = Astro.props;

const dicts = { fr: frDict, ar: arDict, en: enDict };
const dict = dicts[lang] || frDict;

function t(key, fallbackText) {
  return dict[key] !== undefined ? dict[key] : fallbackText;
}

const getRelativePath = (path, targetLang) => {
  if (targetLang === 'fr') return path;
  return `/${targetLang}${path === '/' ? '' : path}`;
};
---

<BaseLayout
  title={`${t('hero_title', 'From Zero to Hero')} | AQA Sports Academy`}
  description={t('hero_desc', 'Elite coaching, swimming, and apnea academy in Algeria.')}
  currentPage="/"
  lang={lang}
>
  <Navbar currentPage="/" lang={lang} />

  <!-- Floating CTA Button -->
  <a href={getRelativePath('/cards', lang)} class="floating-btn" aria-label="Voir les nouvelles formules AQA">
    {t('floating_btn', '✨ Nouvelles formules')}
  </a>

  <!-- Loading Screen -->
  <div class="loading-screen" id="loadingScreen">
    <img src="https://i.imgur.com/3dcksCn.png" alt="AQA Logo" class="loading-logo" />
    <div class="loading-bar-container">
      <div class="loading-bar"></div>
    </div>
    <div class="loading-text">{t('loading_text', 'CHARGEMENT...')}</div>
  </div>

  <div class="container">
    <!-- Hero Section -->
    <section class="hero">
      <h1>{t('hero_title', 'From Zero to Hero')}</h1>
      
      <div class="hero-video">
        <video autoplay muted loop playsinline class="transformation-video">
          <source src="/zero.mp4" type="video/mp4" />
          Your browser does not support the video tag.
        </video>
      </div>
      
      <p>{t('hero_desc', 'AQA est un établissement sportif dédié à transformer les débutants en athlètes confiants...')}</p>
    </section>

    <!-- Stats Section -->
    <section class="section" id="statsSection">
      <div class="stats-grid">
        <div class="stat-card glass-panel" data-delay="0">
          <div class="stat-number" data-target="500" data-suffix="+">0+</div>
          <div class="stat-label">{t('stats_athletes', 'Athlètes formés')}</div>
        </div>
        <div class="stat-card glass-panel" data-delay="100">
          <div class="stat-number" data-target="8" data-suffix="+">0+</div>
          <div class="stat-label">{t('stats_coaches', "Coachs d'élite")}</div>
        </div>
        <div class="stat-card glass-panel" data-delay="200">
          <div class="stat-number" data-target="3" data-suffix="">0</div>
          <div class="stat-label">{t('stats_pools', 'Piscines partenaires')}</div>
        </div>
        <div class="stat-card glass-panel" data-delay="300">
          <div class="stat-number" data-target="98" data-suffix="%">0%</div>
          <div class="stat-label">{t('stats_satisfaction', 'Satisfaction client')}</div>
        </div>
      </div>
    </section>

    <!-- Essential Services -->
    <section class="section">
      <div class="section-header">
        <h2 class="section-title">{t('section_services_title', 'Nos Services Essentiels')}</h2>
        <div class="section-divider"></div>
        <p class="section-subtitle">{t('section_services_sub', 'Tout ce dont vous avez besoin pour commencer votre transformation')}</p>
      </div>
      
      <div class="nav-buttons">
        <a href={getRelativePath('/tarifs', lang)} class="nav-btn glass-panel">
          <div class="nav-btn-icon">
            <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
              <line x1="12" y1="2" x2="12" y2="22"></line>
              <path d="M17 5H9.5a3.5 3.5 0 000 7h5a3.5 3.5 0 010 7H6"></path>
            </svg>
          </div>
          <div>{t('btn_tarifs', 'Tarifs')}</div>
        </a>

        <a href={getRelativePath('/groupes', lang)} class="nav-btn glass-panel">
          <div class="nav-btn-icon">
            <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
              <path d="M2 18c6-6 14-6 20 0"></path>
              <circle cx="12" cy="7" r="3"></circle>
            </svg>
          </div>
          <div>{t('btn_groupes', 'Groupes')}</div>
        </a>

        <a href={getRelativePath('/inscription', lang)} class="nav-btn glass-panel">
          <div class="nav-btn-icon">
            <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
              <path d="M12 20h9"></path>
              <path d="M16.5 3.5a2.121 2.121 0 113 3L7 19l-4 1 1-4 12.5-12.5z"></path>
            </svg>
          </div>
          <div>{t('btn_inscription', 'Inscription')}</div>
        </a>

        <a href={getRelativePath('/infos', lang)} class="nav-btn glass-panel">
          <div class="nav-btn-icon">
            <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
              <circle cx="12" cy="12" r="10"></circle>
              <line x1="12" y1="16" x2="12" y2="12"></line>
              <line x1="12" y1="8" x2="12" y2="8"></line>
            </svg>
          </div>
          <div>{t('btn_infos', 'Infos')}</div>
        </a>
      
        <a href={getRelativePath('/cards', lang)} class="nav-btn glass-panel">
          <div class="nav-btn-icon">
            <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
              <rect x="1" y="4" width="22" height="16" rx="2" ry="2"></rect>
              <line x1="1" y1="10" x2="23" y2="10"></line>
            </svg>
          </div>
          <div>{t('btn_formules', 'Formules AQA')}</div>
        </a>
      </div>
    </section>

    <!-- Services & Education -->
    <section class="section">
      <div class="section-header">
        <h2 class="section-title">{t('section_formations_title', 'Services & Formations')}</h2>
        <div class="section-divider"></div>
        <p class="section-subtitle">{t('section_formations_sub', 'Programmes spécialisés pour tous les niveaux')}</p>
      </div>
      
      <div class="nav-buttons">
        <a href={getRelativePath('/formation', lang)} class="nav-btn glass-panel">
          <div class="nav-btn-icon">
            <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
              <path d="M22 10L12 4 2 10l10 6 10-6z"></path>
              <path d="M6 12v5c0 1.1 2.7 2 6 2s6-.9 6-2v-5"></path>
            </svg>
          </div>
          <div>{t('btn_formations', 'Formations')}</div>
        </a>

        <a href={getRelativePath('/kids', lang)} class="nav-btn glass-panel">
          <div class="nav-btn-icon">
            <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
              <circle cx="12" cy="12" r="7"></circle>
              <circle cx="10" cy="12" r="0.5"></circle>
              <circle cx="14" cy="12" r="0.5"></circle>
              <path d="M9 15c1.5 1 4.5 1 6 0"></path>
            </svg>
          </div>
          <div>{t('btn_kids', 'AQA Kids')}</div>
        </a>

        <a href={getRelativePath('/well', lang)} class="nav-btn glass-panel">
          <div class="nav-btn-icon">
            <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
              <path d="M12 4.5l-1.2-1.3a3.5 3.5 0 0 0-5 5L12 13l6.2-4.8a3.5 3.5 0 0 0-5-5L12 4.5z"/>
              <path d="M3 18c1.5-1 3-2 5-2h6c1.5 0 2.5 1 2.5 2s-1 2-2.5 2H9c-1.5 0-2.5 1-3 2"/>
            </svg>
          </div>
          <div>{t('btn_wellness', 'AQA Wellness')}</div>
        </a>

        <a href={getRelativePath('/events', lang)} class="nav-btn glass-panel">
          <div class="nav-btn-icon">
            <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
              <rect x="3" y="4" width="18" height="18" rx="2"></rect>
              <line x1="16" y1="2" x2="16" y2="6"></line>
              <line x1="8" y1="2" x2="8" y2="6"></line>
              <line x1="3" y1="10" x2="21" y2="10"></line>
            </svg>
          </div>
          <div>{t('btn_events', 'AQA events')}</div>
        </a>
        
        <a href={getRelativePath('/activities', lang)} class="nav-btn glass-panel">
          <div class="nav-btn-icon">
            <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
              <circle cx="12" cy="12" r="10"></circle>
              <path d="M12 2v20M2 12h20"></path>
              <path d="M4 4l16 16M20 4L4 20"></path>
            </svg>
          </div>
          <div>{t('btn_sports', 'Sports')}</div>
        </a>
      </div>
    </section>

    <!-- Structure & Media -->
    <section class="section">
      <div class="section-header">
        <h2 class="section-title">{t('section_media_title', 'Structure et Média')}</h2>
        <div class="section-divider"></div>
        <p class="section-subtitle">{t('section_media_sub', 'Coordination visuelle et communication des activités AQA.')}</p>
      </div>
      
      <div class="nav-buttons">
        <a href={getRelativePath('/piscines', lang)} class="nav-btn glass-panel">
          <div class="nav-btn-icon">
            <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
              <path d="M21 10c0 6-9 12-9 12s-9-6-9-12a9 9 0 1118 0z"></path>
              <circle cx="12" cy="10" r="3"></circle>
            </svg>
          </div>
          <div>{t('btn_piscines', 'Piscines')}</div>
        </a>

        <a href={getRelativePath('/team', lang)} class="nav-btn glass-panel">
          <div class="nav-btn-icon">
            <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
              <circle cx="12" cy="7" r="3"></circle>
              <path d="M9 21v-2a3 3 0 0 1 6 0v2"></path>
              <circle cx="5" cy="9" r="2.5"></circle>
              <path d="M2 21v-2a3 3 0 0 1 6 0v2"></path>
              <circle cx="19" cy="9" r="2.5"></circle>
              <path d="M16 21v-2a3 3 0 0 1 6 0v2"></path>
            </svg>
          </div>
          <div>{t('btn_team', 'Team')}</div>
        </a>

        <a href={getRelativePath('/clients', lang)} class="nav-btn glass-panel">
          <div class="nav-btn-icon">
            <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
              <circle cx="12" cy="7" r="3"></circle>
              <path d="M9 21v-2a3 3 0 0 1 6 0v2"></path>
              <circle cx="5" cy="9" r="2"></circle>
              <path d="M2 21v-2a3 3 0 0 1 6 0v2"></path>
              <circle cx="19" cy="9" r="2"></circle>
              <path d="M16 21v-2a3 3 0 0 1 6 0v2"></path>
            </svg>
          </div>
          <div>{t('btn_adherents', 'Adhérents')}</div>
        </a>

        <a href={getRelativePath('/webtv', lang)} class="nav-btn glass-panel">
          <div class="nav-btn-icon">
            <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
              <rect x="2" y="7" width="14" height="10" rx="2" ry="2"></rect>
              <polygon points="16 10 22 7 22 17 16 14 16 10"></polygon>
            </svg>
          </div>
          <div>{t('btn_webtv', 'WebTV')}</div>
        </a>
      </div>
    </section>

    <!-- Boutique & Technical Supports -->
    <section class="section">
      <div class="section-header">
        <h2 class="section-title">{t('section_boutique_title', 'Boutique et supports technique')}</h2>
        <div class="section-divider"></div>
        <p class="section-subtitle">{t('section_boutique_sub', 'Équipez-vous avec les accessoires et guides AQA')}</p>
      </div>
      
      <div class="nav-buttons">
        <a href={getRelativePath('/wear', lang)} class="nav-btn glass-panel">
          <div class="nav-btn-icon">
            <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
              <path d="M4 7l4-3h8l4 3v4l-2 2v8H6v-8l-2-2V7z"/>
              <path d="M9 4l1.5 2h3L15 4"/>
            </svg>
          </div>
          <div>{t('btn_wear', 'AQA Wear')}</div>
        </a>

        <a href={getRelativePath('/store', lang)} class="nav-btn glass-panel">
          <div class="nav-btn-icon">
            <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
              <path d="M3 9l1-5h16l1 5z" />
              <path d="M4 9a2 2 0 0 0 4 0 2 2 0 0 0 4 0 2 2 0 0 0 4 0" />
              <rect x="3" y="9" width="18" height="12" rx="2" />
              <path d="M10 21v-6h4v6" />
            </svg>
          </div>
          <div>{t('btn_store', 'AQA Store')}</div>
        </a>

        <a href={getRelativePath('/app', lang)} class="nav-btn glass-panel">
          <div class="nav-btn-icon">
            <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
              <rect x="6" y="2" width="12" height="20" rx="2" ry="2"></rect>
              <line x1="12" y1="20" x2="12" y2="20"></line>
            </svg>
          </div>
          <div>{t('btn_app', 'AQA App')}</div>
        </a>
      </div>
    </section>

    <!-- Reviews Section -->
    <section class="section">
      <div class="section-header">
        <h2 class="section-title">{t('section_reviews_title', 'Ce que disent nos athlètes')}</h2>
        <div class="section-divider"></div>
        <p class="section-subtitle">{t('section_reviews_sub', 'Témoignages de transformation réussie')}</p>
      </div>
      
      <div class="reviews-container">
        <div class="review-card glass-panel">
          <div class="review-stars">★★★★★</div>
          <p class="review-text">"واحد من أفضل نوادي تعليم السباحة في جزائر ، Great experience"</p>
          <p class="review-author">- amir abdelaziz</p>
        </div>
        
        <div class="review-card glass-panel">
          <div class="review-stars">★★★★★</div>
          <p class="review-text">"الكلمة الوحيدة التي استطيع ان اقولها عن الفريق و الطاقم التدريبي :شكرا لكم انتم فريق محترف حقيقة!!!"</p>
          <p class="review-author">- Hakim Tazir</p>
        </div>
        
        <div class="review-card glass-panel">
          <div class="review-stars">★★★★★</div>
          <p class="review-text">"افضل نادي سباحة مدربين محترفين ، السباحة على اصولها"</p>
          <p class="review-author">- Raouf Benyakhlef</p>
        </div>
      </div>
    </section>

    <!-- Gallery Section -->
    <section class="section">
      <div class="section-header">
        <h2 class="section-title">{t('section_gallery_title', 'Nos Moments Forts')}</h2>
        <div class="section-divider"></div>
        <p class="section-subtitle">{t('section_gallery_sub', "Découvrez l'esprit AQA en images")}</p>
      </div>
      
      <div class="gallery-grid">
        <div class="gallery-item glass-panel">
          <img src="https://i.ibb.co/ksrvQyDK/IMG20240224224823.jpg?w=400" alt="Training session" loading="lazy" width="400" height="400" />
        </div>
        <div class="gallery-item glass-panel">
          <img src="https://i.ibb.co/9mz245Tp/IMG20240120224811.jpg?w=400" alt="Swimming pool" loading="lazy" width="400" height="400" />
        </div>
        <div class="gallery-item glass-panel">
          <img src="https://i.ibb.co/9JzqTWS/images-1-1-1.jpg?w=400" alt="Competition" loading="lazy" width="400" height="400" />
        </div>
        <div class="gallery-item glass-panel">
          <img src="https://i.ibb.co/tMZ0R873/IMG20250705080230.jpg?w=400" alt="Kids training" loading="lazy" width="400" height="400" />
        </div>
      </div>
    </section>

    <!-- Listen Section Wrapper -->
    <section class="section">
      <div class="listen-wrapper glass-panel">
        <div class="listen-trigger" onclick="toggleListenSection()" id="listenBtn">
          <div class="trigger-text">
            <div class="icon-pulse">
              <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                <path d="M12 1a3 3 0 0 0-3 3v8a3 3 0 0 0 6 0V4a3 3 0 0 0-3-3z"></path>
                <path d="M19 10v2a7 7 0 0 1-14 0v-2"></path>
                <line x1="12" y1="19" x2="12" y2="23"></line>
                <line x1="8" y1="23" x2="16" y2="23"></line>
              </svg>
            </div>
            <span>{t('listen_trigger', 'We Listen — Votre avis compte')}</span>
          </div>
          <svg class="trigger-arrow" xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
            <polyline points="6 9 12 15 18 9"></polyline>
          </svg>
        </div>

        <div class="listen-container" id="listenContent">
          <iframe 
            id="listenFrame"
            src="/listen-section.html" 
            style="width:100%; height:1200px; border:none; display:block;"
            allow="microphone *; camera *"
            allowfullscreen
            sandbox="allow-same-origin allow-scripts allow-forms allow-popups allow-modals allow-downloads"
            title="We Listen Section"
          >
          </iframe>
        </div>
      </div>
    </section>
  </div>

  <Footer lang={lang} />
</BaseLayout>

<script is:inline>
  // Hide loader
  document.addEventListener('astro:page-load', () => {
    setTimeout(() => {
      const loader = document.getElementById('loadingScreen');
      if (loader) loader.classList.add('hidden');
    }, 1200);
  });

  // Intersection Observer for Sections to animate appearance
  const observerOptions = {
    threshold: 0.15,
    rootMargin: '0px'
  };

  const sectionObserver = new IntersectionObserver((entries) => {
    entries.forEach(entry => {
      if (entry.isIntersecting) {
        entry.target.classList.add('visible');
        
        // Stat Cards animate
        entry.target.querySelectorAll('.stat-card').forEach((card, index) => {
          setTimeout(() => {
            card.classList.add('visible');
            const num = card.querySelector('.stat-number');
            if (num && !card.classList.contains('counted')) {
              card.classList.add('counted');
              animateNumber(num);
            }
          }, index * 100);
        });

        // Navigation Buttons animate
        entry.target.querySelectorAll('.nav-btn').forEach((btn, index) => {
          setTimeout(() => {
            btn.classList.add('visible');
          }, index * 40);
        });
      }
    });
  }, observerOptions);

  document.querySelectorAll('.section, .hero').forEach(sec => {
    sectionObserver.observe(sec);
  });

  function animateNumber(element) {
    const target = parseInt(element.getAttribute('data-target') || 0);
    const suffix = element.getAttribute('data-suffix') || '';
    const duration = 2000;
    let current = 0;
    const increment = target / 60;
    
    const timer = setInterval(() => {
      current += increment;
      if (current >= target) {
        element.textContent = target + suffix;
        clearInterval(timer);
      } else {
        element.textContent = Math.floor(current) + suffix;
      }
    }, duration / 60);
  }

  // Toggle We Listen Drawer
  function toggleListenSection() {
    const btn = document.getElementById('listenBtn');
    const content = document.getElementById('listenContent');
    
    btn.classList.toggle('active');
    content.classList.toggle('open');

    if (content.classList.contains('open')) {
      setTimeout(() => {
        content.scrollIntoView({ behavior: 'smooth', block: 'start' });
      }, 300);
    }
  }
</script>

<style>
  /* Local Styles for Home Page */
  .hero {
    text-align: center;
    padding: 3rem 0;
    margin-bottom: 2rem;
    opacity: 0;
    transform: translateY(30px);
    transition: all 0.8s ease;
  }
  .hero.visible {
    opacity: 1;
    transform: translateY(0);
  }

  .hero h1 {
    font-size: clamp(2.5rem, 8vw, 4rem);
    background: linear-gradient(135deg, var(--color-teal), var(--color-blue), #fff);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    margin-bottom: 1.5rem;
    text-shadow: 0 0 35px rgba(0, 242, 255, 0.25);
  }

  .hero-video {
    margin: 2.5rem auto;
    max-width: 680px;
    border-radius: 20px;
    overflow: hidden;
    box-shadow: 0 10px 40px rgba(0, 0, 0, 0.6);
    border: 1px solid rgba(0, 242, 255, 0.2);
    position: relative;
  }

  .transformation-video {
    width: 100%;
    height: auto;
    display: block;
    max-height: 400px;
    object-fit: cover;
    background: #000;
  }

  .hero p {
    font-size: clamp(1.05rem, 3vw, 1.25rem);
    max-width: 800px;
    margin: 0 auto;
  }

  /* Stats Grid */
  .stats-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
    gap: 1.5rem;
    margin: 2rem 0;
  }

  .stat-card {
    padding: 2.2rem;
    text-align: center;
    opacity: 0;
    transform: translateY(30px);
    transition: all 0.6s cubic-bezier(0.4, 0, 0.2, 1);
  }

  .stat-card.visible {
    opacity: 1;
    transform: translateY(0);
  }

  .stat-number {
    font-size: 3.2rem;
    font-weight: 800;
    background: linear-gradient(135deg, var(--color-teal), var(--color-blue));
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
  }

  .stat-label {
    font-size: 1rem;
    color: var(--text-secondary);
    margin-top: 0.5rem;
  }

  /* Section Header */
  .section {
    padding: 3rem 0;
    opacity: 0;
    transform: translateY(40px);
    transition: all 0.8s cubic-bezier(0.4, 0, 0.2, 1);
  }
  
  .section.visible {
    opacity: 1;
    transform: translateY(0);
  }

  .section-header {
    text-align: center;
    margin-bottom: 2.5rem;
  }

  .section-title {
    font-size: clamp(1.8rem, 5vw, 2.3rem);
    background: linear-gradient(135deg, var(--color-teal), var(--color-blue));
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    margin-bottom: 0.5rem;
  }

  .section-subtitle {
    font-size: 1.05rem;
    color: var(--text-secondary);
  }

  .section-divider {
    width: 60px;
    height: 3px;
    background: var(--color-teal);
    margin: 1rem auto;
    border-radius: 2px;
    box-shadow: 0 0 10px rgba(0, 242, 255, 0.5);
  }

  /* Nav Buttons Grid */
  .nav-buttons {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(170px, 1fr));
    gap: 1.25rem;
    max-width: 1000px;
    margin: 0 auto;
  }

  .nav-btn {
    padding: 1.8rem 1rem;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    min-height: 120px;
    opacity: 0;
    transform: translateY(20px);
    transition: all 0.5s ease;
  }

  .nav-btn.visible {
    opacity: 1;
    transform: translateY(0);
  }

  .nav-btn:hover {
    transform: translateY(-5px);
    border-color: var(--color-teal);
    box-shadow: var(--shadow-glow-active);
  }

  .nav-btn-icon svg {
    width: 32px;
    height: 32px;
    stroke: var(--color-teal);
    filter: drop-shadow(0 0 8px rgba(0, 242, 255, 0.4));
  }

  .nav-btn div {
    margin-top: 0.75rem;
    font-weight: 600;
    font-size: 0.95rem;
  }

  /* Reviews Section */
  .reviews-container {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
    gap: 1.5rem;
    max-width: 1100px;
    margin: 0 auto;
  }

  .review-card {
    padding: 2rem 1.5rem;
    display: flex;
    flex-direction: column;
    justify-content: space-between;
  }

  .review-stars {
    color: var(--color-teal);
    font-size: 1.15rem;
    margin-bottom: 0.8rem;
    text-shadow: 0 0 8px rgba(0, 242, 255, 0.4);
  }

  .review-text {
    font-style: italic;
    color: var(--text-primary);
    line-height: 1.5;
  }

  .review-author {
    margin-top: 1rem;
    text-align: right;
    color: var(--color-teal);
    font-weight: 600;
    font-size: 0.9rem;
  }

  /* Gallery Section */
  .gallery-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
    gap: 1.25rem;
  }

  .gallery-item {
    aspect-ratio: 1;
    overflow: hidden;
    padding: 0.25rem;
  }

  .gallery-item img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    border-radius: 16px;
    transition: var(--transition-smooth);
  }

  .gallery-item:hover img {
    transform: scale(1.06);
  }

  /* We Listen Section Drawer */
  .listen-wrapper {
    max-width: 760px;
    margin: 0 auto;
    overflow: hidden;
    padding: 0;
  }

  .listen-trigger {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 1.5rem;
    cursor: pointer;
    font-weight: 700;
    transition: var(--transition-smooth);
    background: rgba(255,255,255,0.01);
  }

  .listen-trigger:hover {
    background: rgba(0, 242, 255, 0.03);
  }

  .trigger-text {
    display: flex;
    align-items: center;
    gap: 0.75rem;
    font-size: 1.1rem;
    color: var(--color-teal);
  }

  .icon-pulse svg {
    animation: pulse 2s infinite;
  }

  .trigger-arrow {
    transition: var(--transition-smooth);
  }

  .listen-trigger.active .trigger-arrow {
    transform: rotate(180deg);
  }

  .listen-container {
    max-height: 0;
    overflow: hidden;
    transition: max-height 0.4s cubic-bezier(0.4, 0, 0.2, 1);
  }

  .listen-container.open {
    max-height: 1300px;
  }

  /* Loading Animations */
  .loading-screen {
    position: fixed;
    top: 0;
    left: 0;
    width: 100vw;
    height: 100vh;
    background: #030712;
    z-index: 9999;
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: center;
    transition: opacity 0.5s ease, visibility 0.5s ease;
  }

  .loading-screen.hidden {
    opacity: 0;
    visibility: hidden;
  }

  .loading-logo {
    width: 120px;
    height: 120px;
    margin-bottom: 2rem;
    animation: neonGlow 3s ease-in-out infinite alternate;
  }

  .loading-bar-container {
    width: 260px;
    height: 4px;
    background: rgba(0, 242, 255, 0.1);
    border-radius: 10px;
    overflow: hidden;
  }

  .loading-bar {
    height: 100%;
    background: linear-gradient(90deg, var(--color-blue), var(--color-teal));
    width: 100%;
    transform: translateX(-100%);
    animation: loadingBarRun 1.5s cubic-bezier(0.4, 0, 0.2, 1) infinite;
  }

  @keyframes loadingBarRun {
    0% { transform: translateX(-100%); }
    50% { transform: translateX(0%); }
    100% { transform: translateX(100%); }
  }

  .loading-text {
    margin-top: 1rem;
    color: var(--color-teal);
    font-size: 0.85rem;
    letter-spacing: 2px;
  }

  .floating-btn {
    position: fixed;
    bottom: 20px;
    right: 20px;
    background: linear-gradient(135deg, var(--color-blue), var(--color-teal));
    color: #030712;
    padding: 0.8rem 1.5rem;
    border-radius: 50px;
    font-weight: 700;
    box-shadow: 0 4px 20px rgba(0, 242, 255, 0.25);
    border: 1px solid var(--color-teal);
    z-index: 1000;
  }

  .floating-btn:hover {
    transform: translateY(-2px);
    box-shadow: 0 8px 30px rgba(0, 242, 255, 0.45);
  }

  /* RTL Support overrides */
  :global([dir="rtl"]) .listen-trigger {
    flex-direction: row-reverse;
  }
  :global([dir="rtl"]) .trigger-text {
    flex-direction: row-reverse;
  }
  :global([dir="rtl"]) .floating-btn {
    right: auto;
    left: 20px;
  }
</style>
