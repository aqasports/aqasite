---
export function getStaticPaths() {
  return [
    { params: { lang: 'ar' } },
    { params: { lang: 'en' } }
  ];
}

const { lang } = Astro.params;
import ApneaPagePage from '../apnea_page.astro';
---

<ApneaPagePage lang={lang} />
