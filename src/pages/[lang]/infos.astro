---
export function getStaticPaths() {
  return [
    { params: { lang: 'ar' } },
    { params: { lang: 'en' } }
  ];
}

const { lang } = Astro.params;
import InfosPage from '../infos.astro';
---

<InfosPage lang={lang} />
