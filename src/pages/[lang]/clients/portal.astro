---
export function getStaticPaths() {
  return [
    { params: { lang: 'ar' } },
    { params: { lang: 'en' } }
  ];
}

const { lang } = Astro.params;
import PortalPage from '../../clients/portal.astro';
---

<PortalPage lang={lang} />
