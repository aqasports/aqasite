---
export function getStaticPaths() {
  return [
    { params: { lang: 'ar' } },
    { params: { lang: 'en' } }
  ];
}

const { lang } = Astro.params;
import StatsPage from '../../clients/stats.astro';
---

<StatsPage lang={lang} />
