---
export function getStaticPaths() {
  return [
    { params: { lang: 'ar' } },
    { params: { lang: 'en' } }
  ];
}

const { lang } = Astro.params;
import ClientsPage from '../clients.astro';
---

<ClientsPage lang={lang} />
