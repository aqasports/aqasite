---
export function getStaticPaths() {
  return [
    { params: { lang: 'ar' } },
    { params: { lang: 'en' } }
  ];
}

const { lang } = Astro.params;
import WebtvPage from '../webtv.astro';
---

<WebtvPage lang={lang} />
