---
export function getStaticPaths() {
  return [
    { params: { lang: 'ar' } },
    { params: { lang: 'en' } }
  ];
}

const { lang } = Astro.params;
import InfrastructurePage from '../infrastructure.astro';
---

<InfrastructurePage lang={lang} />
