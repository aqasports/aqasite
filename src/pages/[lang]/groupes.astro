---
export function getStaticPaths() {
  return [
    { params: { lang: 'ar' } },
    { params: { lang: 'en' } }
  ];
}

const { lang } = Astro.params;
import GroupesPage from '../../components/pages/GroupesPage.astro';
---

<GroupesPage lang={lang} />
