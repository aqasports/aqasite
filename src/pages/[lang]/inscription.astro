---
export function getStaticPaths() {
  return [
    { params: { lang: 'ar' } },
    { params: { lang: 'en' } }
  ];
}

const { lang } = Astro.params;
import InscriptionPage from '../inscription.astro';
---

<InscriptionPage lang={lang} />
