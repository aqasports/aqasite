---
export function getStaticPaths() {
  return [
    { params: { lang: 'ar' } },
    { params: { lang: 'en' } }
  ];
}

const { lang } = Astro.params;
import MerciPage from '../merci.astro';
---

<MerciPage lang={lang} />
